defaults:
  - default

accelerator: gpu
devices: 1
gradient_clip_val: 1.0
# Accumulate micro-batches to emulate a larger effective batch size
# Example: data.batch_size=4 with accumulate_grad_batches=8 -> effective 32
accumulate_grad_batches: 8
